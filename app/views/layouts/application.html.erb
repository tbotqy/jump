<!DOCTYPE html>
<% if @show_scrollbar %>
  <html lang="ja" class="show-scrollbar">
<% else %>
  <html lang="ja">
<% end %>
  <head>
    <title><%= title @title %></title>
    <meta charset="UTF-8"/>
    <meta name="viewport" content="width=device-width">
    <% if configatron.site_mode == "production" %>
      <meta name="robots" content="index,follow"/>
    <% else %>
      <meta name="robots" content="noindex,nofollow,nocache"/>
    <% end %>
    <%= favicon_link_tag %>
    <meta name="description" content="あの日のタイムラインを眺められる、ちょっとしたアプリケーション"/>
    <meta name="keywords" content="タイムライン,過去のタイムライン,過去のつぶやき,過去のツイート"/>
    <meta property="og:locale" content="ja_JP">
    <meta property="fb:app_id" content="258025897640441" />
    <meta property="og:site_name" content="Timdeline" />
    <meta property="og:title" content="<%= title @title %>" />
    <meta property="og:description" content="Timedline is a simple view system for those who want to have the access to their past timeline." />
    <meta property="og:url" content="<%= request.url %>" />
    <meta property="og:type" content="website" />
    <meta property="og:image" content="<%= request.url %>/favicon.ico" />
    <!--[if lt IE 9]>
    <script src="http://html5shim.googlecode.com/svn/trunk/html5.js" type="text/javascript"></script>
    <![endif]-->    
    <link href='http://fonts.googleapis.com/css?family=Contrail+One|Fugaz+One|Emblema+One|Questrial|PT+Sans' rel='stylesheet' type='text/css'>
    <% if configatron.site_mode == "production" %>
      <% if request.smart_phone? %>
	<link href="/assets/smart_phone/application.css?v=1" media="all" rel="stylesheet" type="text/css" />
	<script src="/assets/smart_phone/application.js?v=1" type="text/javascript"></script>
      <% else %>
	<link href="/assets/application.css?v=3.1" media="all" rel="stylesheet" type="text/css" />
	<script src="/assets/application.js?v=2.4" type="text/javascript"></script>
      <% end %>
    <% else %>
      <% if request.smart_phone? %>
      	<link href="/assets/smart_phone/application.css?v=<%= Time.now.to_i %>" media="all" rel="stylesheet" type="text/css" />
	<script src="/assets/smart_phone/application.js?v=<%= Time.now.to_i %>" type="text/javascript"></script>
      <% else %>
	<link href="/assets/application.css?v=<%= Time.now.to_i %>" media="all" rel="stylesheet" type="text/css" />
	<script src="/assets/application.js?v=<%= Time.now.to_i %>" type="text/javascript"></script>
      <% end %>
    <% end %>
    <%= csrf_meta_tags %>
    <% if configatron.site_mode == "production" %>
    <script type="text/javascript">
      var _gaq = _gaq || [];
      _gaq.push(['_setAccount', 'UA-28886746-8']);
      _gaq.push(['_trackPageview']);
      
      (function() {
      var ga = document.createElement('script'); ga.type = 'text/javascript'; ga.async = true;
      ga.src = ('https:' == document.location.protocol ? 'https://ssl' : 'http://www') + '.google-analytics.com/ga.js';
      var s = document.getElementsByTagName('script')[0]; s.parentNode.insertBefore(ga, s);
      })();
    </script>
    <% end %>
  </head>
  <body>
    <div id="fb-root"></div>
    <%= render "shared/global_header",{ :current_user => @current_user || false} if @show_header == true  %>
    <!-- #container-main -->
    <div id="container-main" class="<%= controller.action_name %>">
      <div class="inner">
	<%= yield %>
      </div>
    </div>
    <!-- /#container-main -->
    <%= render "shared/to_page_top" %>
    <% if @show_footer %>
    <%= render "shared/footer" %>
    <% end %>
  </body>
</html>
